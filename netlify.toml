[build]
  command = "pip install -r docs/requirements.txt && mkdocs build --config-file mkdocs.yml"
  publish = "site"
  ignore = "git diff --quiet \"$CACHED_COMMIT_REF\" \"$COMMIT_REF\" -- docs/ mkdocs.yml"

[build.environment]
  PYTHON_VERSION = "3.11"